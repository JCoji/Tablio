# Audio I/O
librosa==0.10.1              # Audio loading and CQT feature extraction
soundfile==0.12.1            # WAV file I/O
audioread==3.0.1             # Audio decoding

# Core dependencies
numpy==1.24.3                # Numerical computing
scipy==1.11.4                # Signal processing

# Deep learning
torch                        # Model inference (install CPU or CUDA build as appropriate)

# MIDI / evaluation
pretty_midi                  # MIDI export
mir_eval                     # Note transcription evaluation metrics

# Utilities
tqdm==4.66.1                 # Progress bars

# Note: Demucs itself runs in Docker, so not included here
